import { server } from "../../framework.js";
const { defineService } = server;

export const useAuthService = defineService("auth", {
  get: () => {},
  post: () => {},
  put: () => {},
  delete: () => {},
});
